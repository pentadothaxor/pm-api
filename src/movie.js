const MovieClass = {
    id: 'string',
    name: 'string',
    creationYear: 'date_year',
    creationDate: 'date',
    genres: 'array[string]',
    tvShow: 'boolean',
};

const add = async (params) => {};

const remove = async (params) => {};

const update = async (params) => {};

const addComment = async (params) => {};

const addRating = async (params) => {};

const get = async (params) => {

}

const getAll = async (params) => {

}

export default {
    add,
    remove,
    update,
    addComment,
    addRating,
    get, getAll
};
